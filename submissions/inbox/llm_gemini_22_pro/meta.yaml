model: GATConv
type: llm
notes: GNN-CB LLM baseline. Generated by Gemini 2.5 Pro (gemini-2.5-pro) under frozen prompt template prompt.md v1.1 (temperature=0, top_p=0.99, max_output_tokens=10000, seed=42). API-only mode with execute-and-repair loop (repairs used 2/5). Zero few-shot, zero hints, zero edits beyond auto-repair. Pipeline -- 3-layer GATConv hidden=64 heads=4 dropout=0.6 with BatchNorm after each conv (layers 1 and 2 multi-head, final layer single-head concat=False); mean+max pool concatenated as graph embedding; 2-layer MLP head (Linear -> ReLU -> Linear); AdamW lr=1e-3 weight_decay=5e-4; 150 epochs full-batch training; CrossEntropyLoss. CPU only, total elapsed 183.80s.
